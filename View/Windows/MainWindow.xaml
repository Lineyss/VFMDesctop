<Window x:Class="VFMDesctop.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VFMDesctop.View" 
        xmlns:windows="clr-namespace:VFMDesctop.ViewModels.Windows"
        mc:Ignorable="d"
        MinHeight="430" MinWidth="350" MaxHeight="430" MaxWidth="350">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Panel.ZIndex="2">
            <Button Name="exit" 
                    Grid.Column="1" 
                    Command="{Binding exitButton_mouseClick}">
                <Button.Background>
                    <SolidColorBrush Color="#FFDDDDDD" Opacity="0"/>
                </Button.Background>
                <Button.BorderBrush>
                    <SolidColorBrush Color="#FF707070" Opacity="0"/>
                </Button.BorderBrush>
                <Image Source="/Images/Exit.ico" Height="30" Width="44"/>
            </Button>
        </StackPanel>

        <Grid Grid.Row="1">
            
            
            
        </Grid>
        
    </Grid>

</Window>
